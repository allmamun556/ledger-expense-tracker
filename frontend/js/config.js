/* ============================================================
   Ledger — frontend configuration
   Edit these two values for your own setup.
   ============================================================ */

// Where the FastAPI backend is running.
window.LEDGER_API_BASE = "http://127.0.0.1:8000";

// Your Google OAuth Web Client ID (from Google Cloud Console).
// Leave blank to hide the "Sign in with Google" button.
// Must match GOOGLE_CLIENT_ID in backend/.env
window.LEDGER_GOOGLE_CLIENT_ID = "";
